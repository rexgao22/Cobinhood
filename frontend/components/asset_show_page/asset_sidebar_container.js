import {connect} from 'react-redux';
import {deleteHolding, sellAsset, buyAsset,buyNewAsse, createHolding} from '../../actions/asset_actions'
import AssetSidebar from './asset_sidebar'

const mapStateToProps = (state, ownProps) => ({

})

